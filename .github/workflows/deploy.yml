name: Deploy Frontend

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy frontend
        run: |
          cd /var/www/ticketera-frontend
          git pull origin main
          npm install
          npm run build